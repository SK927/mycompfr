<?php

  define( 'ADMINS_ID', array(
                          'MY WCA ID',
                        ));

  define( 'NEED_ADMIN', array(
                        'Tool folder #1',
                        'Tool folder #2',
                      ) );

  define( 'NEED_EMAIL', array(
                        'Tool folder #1',
                        'Tool folder #2',
                      ) );

  define( 'NEED_DOB', array(
                        'Tool folder #1',
                        'Tool folder #2',
                    ) );
  
?>